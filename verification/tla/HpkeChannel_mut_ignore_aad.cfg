\* MUTATION CHECK, deliberately broken receiver (expected to FAIL AcceptedIsPrefixOfSealed):
\* the AEAD does not authenticate the aad.
CONSTANTS
    MaxSeq = 5
    AADs = {a1, a2}
    Msgs = {m1, m2, m3}
    Mutation = "ignore_aad"

SPECIFICATION Spec

INVARIANTS
    AcceptedIsPrefixOfSealed
