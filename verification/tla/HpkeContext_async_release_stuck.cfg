\* OBSERVATION (expected to FAIL BusyReleased): in the shipped Fun.protect,
\* an exception raised where the exception path allocates the raw backtrace,
\* before ~finally runs, also leaves busy = TRUE forever.
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"seal", "open", "export"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = FALSE
    AsyncExnBeforeRelease = TRUE
    IncrementOrder = "lsb_first"
    BusyRelease = "fun_protect"
    RfcErrorOrder = FALSE
    Mutation = "none"

SPECIFICATION FairSpec

PROPERTIES
    BusyReleased
