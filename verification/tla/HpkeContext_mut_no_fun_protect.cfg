\* MUTATION CHECK, deliberately broken code (expected to FAIL BusyReleased):
\* busy is released on normal return only, not when the AEAD raises.
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"seal"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = FALSE
    AsyncExnBeforeRelease = FALSE
    IncrementOrder = "lsb_first"
    BusyRelease = "fun_protect"
    RfcErrorOrder = FALSE
    Mutation = "no_fun_protect"

SPECIFICATION FairSpec

PROPERTIES
    BusyReleased
