\* MUTATION CHECK, deliberately broken code (expected to FAIL Refinement):
\* the sequence advances before the AEAD call, so a rejected open advances it.
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"open"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = FALSE
    RfcErrorOrder = FALSE
    Mutation = "incr_before_aead"

SPECIFICATION Spec

PROPERTIES
    Refinement
