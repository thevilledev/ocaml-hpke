\* FINDING (expected to FAIL NoNonceReuse): an asynchronous exception at the
\* poll point in the prologue of the recursive `increment` tears the
\* multi-byte increment of state.sequence.
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"seal"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = TRUE
    RfcErrorOrder = FALSE
    Mutation = "none"

SPECIFICATION Spec

INVARIANTS
    NoNonceReuse
