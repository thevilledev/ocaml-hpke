\* MUTATION CHECK, deliberately broken code (expected to FAIL NoNonceReuse):
\* no busy flag, so two domains read the same sequence number.
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"seal"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = FALSE
    RfcErrorOrder = FALSE
    Mutation = "no_cas"

SPECIFICATION Spec

INVARIANTS
    NoNonceReuse
