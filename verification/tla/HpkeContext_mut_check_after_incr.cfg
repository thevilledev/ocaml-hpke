\* MUTATION CHECK, deliberately broken code (expected to FAIL NeverUsesAllOnesNonce):
\* the exhaustion check runs after increment_sequence (on the new value).
CONSTANTS
    Domains = {d1}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"seal"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = FALSE
    RfcErrorOrder = FALSE
    Mutation = "check_after_incr"

SPECIFICATION Spec

INVARIANTS
    NeverUsesAllOnesNonce
