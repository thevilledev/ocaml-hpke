\* MUTATION CHECK, deliberately broken code (expected to FAIL NoNonceReuse):
\* busy is released before increment_sequence.
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"seal"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = FALSE
    AsyncExnBeforeRelease = FALSE
    IncrementOrder = "lsb_first"
    BusyRelease = "fun_protect"
    RfcErrorOrder = FALSE
    Mutation = "release_before_incr"

SPECIFICATION Spec

INVARIANTS
    NoNonceReuse
