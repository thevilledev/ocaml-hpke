\* FINDING (expected to FAIL Refinement): with RfcErrorOrder = TRUE the
\* abstract context demands the exact error of the RFC 9180 pseudocode at
\* seq = MaxSeq, where the AEAD runs before IncrementSeq raises.
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"seal", "open", "export"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = FALSE
    RfcErrorOrder = TRUE
    Mutation = "none"

SPECIFICATION Spec

PROPERTIES
    Refinement
