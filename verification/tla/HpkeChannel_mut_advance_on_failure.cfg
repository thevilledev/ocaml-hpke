\* MUTATION CHECK, deliberately broken receiver (expected to FAIL NoDesync):
\* a rejected open still advances the receiver (increment before the AEAD succeeds).
CONSTANTS
    MaxSeq = 5
    AADs = {a1, a2}
    Msgs = {m1, m2, m3}
    Mutation = "advance_on_failure"

SPECIFICATION Spec

INVARIANTS
    NoDesync
