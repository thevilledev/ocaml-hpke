\* EXPECTED TO FAIL SeqCountsSuccesses: with both fixes, an interrupted
\* carry-first increment leaves the sequence ahead of the number of
\* successful calls. The skipped nonces are never used.
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"seal"}
    AsyncExnAtCas = TRUE
    AsyncExnInIncrement = TRUE
    AsyncExnBeforeRelease = TRUE
    IncrementOrder = "carry_first"
    BusyRelease = "direct"
    RfcErrorOrder = FALSE
    Mutation = "none"

SPECIFICATION Spec

INVARIANTS
    SeqCountsSuccesses
