\* FINDING (expected to FAIL NoNonceReuse): the torn increment on a receiver
\* context. Its sequence goes back, so a replayed ciphertext is accepted
\* a second time.
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"open"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = TRUE
    RfcErrorOrder = FALSE
    Mutation = "none"

SPECIFICATION Spec

INVARIANTS
    NoNonceReuse
