\* Expected to FAIL RelaxedRefinement: the shipped increment under
\* asynchronous exceptions moves seq back, which even the relaxed abstract
\* context forbids (compare HpkeContext_fix_seq_async_incr.cfg).
CONSTANTS
    Domains = {d1, d2}
    SeqBase = 2
    SeqWidth = 2
    Ops = {"seal"}
    AsyncExnAtCas = FALSE
    AsyncExnInIncrement = TRUE
    AsyncExnBeforeRelease = FALSE
    IncrementOrder = "lsb_first"
    BusyRelease = "fun_protect"
    RfcErrorOrder = FALSE
    Mutation = "none"

SPECIFICATION Spec

PROPERTIES
    RelaxedRefinement
