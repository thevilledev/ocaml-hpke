name: CI

on:
  push:
  pull_request:
  schedule:
    - cron: "17 4 * * 1"

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            ocaml: "4.14"
          - os: ubuntu-latest
            ocaml: "5.2"
          - os: ubuntu-latest
            ocaml: "5.4"
          - os: macos-latest
            ocaml: "4.14"
          - os: macos-latest
            ocaml: "5.4"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: ${{ matrix.ocaml }}
          dune-cache: true
      - run: opam install . --deps-only --with-test --with-doc
      - run: opam exec -- dune build @all @doc
      - run: opam exec -- dune runtest
      - run: opam exec -- dune build --profile fuzz fuzz/fuzz_hpke.exe
      - run: opam exec -- _build/default/fuzz/fuzz_hpke.exe --repeat 1000 --seed 9180

  hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: "5.4"
          dune-cache: true
      - run: opam install . --deps-only --with-test --with-doc --with-dev-setup
      - run: opam lint hpke.opam
      - run: opam exec -- dune fmt
      - run: git diff --exit-code

  lower-bounds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ocaml/setup-ocaml@v3
        with:
          ocaml-compiler: "4.14"
          dune-cache: true
      - name: Install declared lower bounds
        run: |
          opam pin add hpke.dev . --no-action
          opam install --yes \
            dune.3.12.1 \
            mirage-crypto.2.4.0 \
            mirage-crypto-ec.2.4.0 \
            mirage-crypto-rng.2.4.0 \
            kdf.1.1.0 digestif.1.3.1 eqaf.0.10 \
            alcotest.1.7.0 qcheck-alcotest.0.25 crowbar.0.2
          opam install --yes --deps-only --with-test hpke.dev
      - run: opam exec -- dune build @all
      - run: opam exec -- dune runtest
